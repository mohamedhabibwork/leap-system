'use client';

import { useRouter } from 'next/navigation';
import CourseEditPage from '../[id]/page';

export default function NewCoursePage() {
  // This page will be handled by the [id] route with id='new'
  return null;
}
