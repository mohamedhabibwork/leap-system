# ============================================
# WEB APPLICATION ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env.local and fill in your values
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# ============================================
# ENVIRONMENT
# ============================================

NODE_ENV=development

# ============================================
# API CONFIGURATION
# ============================================

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# GraphQL API URL
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:3000/graphql

# WebSocket URL (for real-time features)
NEXT_PUBLIC_WS_URL=http://localhost:3000

# gRPC-Web URL (for gRPC communication)
NEXT_PUBLIC_GRPC_WEB_URL=http://localhost:5000

# ============================================
# SITE CONFIGURATION
# ============================================

# Site URL (used for SEO, email links, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Site Name
NEXT_PUBLIC_SITE_NAME=LEAP PM

# Twitter Handle (for social sharing)
NEXT_PUBLIC_TWITTER_HANDLE=@leappm

# ============================================
# AUTHENTICATION & AUTHORIZATION
# ============================================

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl-rand-base64-32

# Session Configuration
SESSION_MAX_AGE=604800
# 604800 = 7 days in seconds
# 2592000 = 30 days in seconds

# ============================================
# OAUTH PROVIDERS (Optional)
# ============================================

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Facebook OAuth
FACEBOOK_CLIENT_ID=your-facebook-client-id
FACEBOOK_CLIENT_SECRET=your-facebook-client-secret

# OIDC Provider (Your own OIDC server)
# These values are seeded automatically when you run the database seeder
# Check the seeder output for the generated client secret
OIDC_ISSUER=http://localhost:3000  # OIDC issuer URL (usually same as NEXT_PUBLIC_API_URL)
OIDC_CLIENT_ID=leap-frontend  # Client ID from OIDC server (seeded by default)
OIDC_CLIENT_SECRET=  # Client secret from OIDC server (generated during seeding)

# ============================================
# FIREBASE CONFIGURATION (Required for Push Notifications)
# ============================================

# Firebase Web App Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Firebase VAPID Key (for Web Push Notifications)
# Get this from Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
NEXT_PUBLIC_FIREBASE_VAPID_KEY=your-vapid-key

# ============================================
# PAYMENT PROCESSING
# ============================================

# Mock payment is used by default (no configuration needed)

# ============================================
# SEO & SEARCH ENGINE VERIFICATION (Optional)
# ============================================

# Google Search Console Verification
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Bing Webmaster Tools Verification
NEXT_PUBLIC_BING_VERIFICATION=

# Yandex Webmaster Verification
NEXT_PUBLIC_YANDEX_VERIFICATION=

# ============================================
# ANALYTICS (Optional)
# ============================================

# Google Analytics 4 Measurement ID
# Get this from Google Analytics > Admin > Data Streams
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Custom Analytics Endpoint
# External endpoint for Web Vitals and custom metrics
NEXT_PUBLIC_ANALYTICS_ENDPOINT=

# Analytics Feature Flags
NEXT_PUBLIC_ANALYTICS_ENABLED=true
NEXT_PUBLIC_ANALYTICS_DEBUG=false
NEXT_PUBLIC_ANALYTICS_SAMPLE_RATE=1.0
# Sample rate: 1.0 = track all events, 0.5 = track 50% of events

# ============================================
# ERROR TRACKING (Optional)
# ============================================

# Sentry DSN (for error tracking and monitoring)
# Get this from Sentry.io > Settings > Projects > Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=

# Sentry Configuration (for build-time integration)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# ============================================
# NOTES
# ============================================
# 
# 1. All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
#    and should not contain sensitive information.
#
# 2. For production, ensure:
#    - NODE_ENV=production
#    - All URLs use HTTPS
#    - Strong secrets for NEXTAUTH_SECRET
#    - Analytics and error tracking are properly configured
#
# 3. Generate NEXTAUTH_SECRET:
#    openssl rand -base64 32
#
# 4. Firebase Configuration:
#    - Create a Firebase project at https://console.firebase.google.com
#    - Add a Web app to your project
#    - Copy the configuration values from Project Settings
#    - Enable Cloud Messaging and generate a VAPID key
#
# 5. For local development, you can leave optional variables empty
#    but ensure all required variables are set.
